const rowStyle = {
  width: '100%',
  display: 'flex',
  flexFlow: 'row wrap',
};
const colStyle = {
  marginBottom: '16px',
};
const gutter = 16;

const textCenter = {
  textAlign: 'center'
}

const basicStyle = {
  rowStyle,
  colStyle,
  gutter,
  textCenter
};

export default basicStyle;
